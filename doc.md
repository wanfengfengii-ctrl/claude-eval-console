CC/Codex 用户反馈标注说明（26.09）
一、项目背景
我们希望还原用户在真实使用 Coding Agent 产品过程中的反馈信号，以判断模型是否真正满足了用户需求，并基于这些数据进行后续模型训练与持续优化。为此，我们计划标注一批真实用户在使用 Coding Agent 过程中的环境、轨迹与高质量用户反馈数据，共建 Coding Agent 能力。
二、项目周期
09/07 ～ 10/07：每个团队产出不少于 30000 条数据。除爬坡或模型无法提供服务的特殊情况外，每个团队日产量不应低于 1000 条，不应高于 3000 条。
一轮对话对应一条数据。
当天 20:00 前执行任务产生的数据必须当天提交，20:00 后产生的数据允许次日 14:00 前提交。
所有数据不允许返修，不符合质量要求的数据将直接拒收。
若专家被项目方抽检发现高频不合格数据，这位专家的历史数据全部拒收，不允许继续参与项目。
三、交付要求
交付字段说明
提交字段	填写说明
提交与题目信息
任务类型	单选，取值与判定口径见「第二步」的任务类型表。按本轮对话的主要意图归类，一轮只选一个。
任务难度	单选，从人类执行的视角判断，分级标准见「第二步」的任务难度表。首轮任务严禁提交「简单」；后续轮次中因模型产物质量不佳而产生的简单 bugfix 可以提交。
语言/框架	本轮实际涉及的主要语言与框架，多个用逗号分隔，例如 Go, Gin, PostgreSQL。
运行环境（同一会话各轮填同一组值）
Harness	单选，承载模型的客户端（Claude Code 或 Codex CLI）。它决定 system prompt、工具集和 agent loop，因此同一模型在不同 Harness 下的表现不能直接互比。
Harness 版本	客户端版本号。Harness 升级会改动 system prompt 与工具集，不记录版本则跨期差异无法解释。
操作系统	单选，本轮作业所在的操作系统环境（MacOS/Linux 或 Windows）。
环境可复现等级	单选，标注这道题的环境能否被评测团队重跑，用于筛选可做同题双跑的样本。「无外部依赖」指 clone 后即可运行；「有外部依赖，未容器化」指依赖数据库、外部服务或本机特定配置，无法直接重跑；「已容器化，可一键起环境」指仓库内提供 docker-compose 或等价的一键启动方式。
初始环境快照	会话首轮开始前的工作区 commit permalink，格式见下方「快照与仓库要求」。整个会话只需这一个快照，各轮填同一个值。
会话与轨迹定位
User Prompt	本轮发给模型的完整 prompt 原文，直接粘贴，不要摘要或改写。带附件、图片或选中代码时，在末尾补一句说明。
SessionID	整个会话窗口的 ID。同一道题的所有轮次填同一个值，用于把多轮记录聚合回一道题。
TurnID/PromptID	本轮对话的唯一 ID，用于把记录精确定位到这一轮的轨迹。Codex CLI 取本轮 task_started 事件的 turn_id；Claude Code 取本轮 user 消息的 promptId。同一 SessionID 下各轮取值互不相同。
轨迹文件	~/.codex/sessions/ 或 ~/.claude/projects/
五维打分与依据描述
打分维度的考察对象与各档锚点见下方「第三步」的评分表，此处不再重复。五个打分字段均填 1 到 5 的整数；五个「- 描述」字段均为必填，须写出可核验的具体依据，不接受「表现一般」「基本可用」这类无法核验的表述，写法见文末「如何写好不满意原因」。
交付完整性	1-5.评分细则见下文.描述中指明具体的文件、报错信息或未实现的需求点。判定虚假成功时，说明模型宣称改了什么、实际改了什么。可以参考"如何写好不满意原因"的产物部分。
指令遵循	1-5.评分细则见下文.描述中列出被违背的约束原文，以及模型实际做了什么。给满分时写一句说明已逐条核对全部约束。
任务规划	1-5.评分细则见下文.描述中说明拆解或阶段性状态追踪在哪一步出了问题。不同 Harness 的计划类工具形态不同，只写计划与状态更新本身的问题，不评价用了哪个具体工具。
推理能力	1-5.评分细则见下文.描述中写清它在哪里想错或想偏了。涉及思考量问题时，明确区分是过度思考还是思考不足。
执行能力	1-5.评分细则见下文.描述中指出具体的冗余或失败调用，例如被重复读取的文件名、反复失败的命令。
其他问题	描述未被上述五维覆盖的其他问题。
快照与仓库要求
快照格式：初始环境快照填 commit permalink，格式为 https://github.com/<org>/<repo>/commit/<40 位完整 SHA>。使用完整 SHA，不要填短 SHA、分支名或 tag：分支和 tag 会随后续提交移动，事后无法还原当时的状态。仓库需要 push 到评测团队可访问的远端，push 到个人私有仓库等同于没有记录。已提交的快照不要再做 force-push 或 rebase，历史改写会让记录的 SHA 变成不可达对象。
凭据不要进仓库：提交工作区前确认 .gitignore 已覆盖 .env 以及各类密钥、连接串和 token 文件。需要说明外部依赖时，只写类型与版本，不要写真实连接串、账号或 token。
整个会话只提交一次快照：在首轮开始前提交一次即可，这意味着模型产出的代码变化完全由轨迹里的工具调用记录承载，因此 SessionID 与 TurnID/PromptID 必须准确回填：这两个字段一旦填错或漏填，这一轮的产物就再也无从追溯，会因为无法质检而打回。根据团队内部管理需要，也可以让大家每一轮都提交一下产物，目前不强制，可以写一个自动提交的hook来提效。
四、作业流程
第一步：接入模型
我们提供的内部模型已经在网关放行了 1M context window，在下面给出的配置脚本里，也对客户端设置了 1M conext window，本期作业请始终保持 max context token = 1000000，请勿自行修改配置，作业前请所有专家自查 context 确认无误后再开始。
配置脚本：
暂时无法在飞书文档外展示此内容
每个team的baseurl，key不一样，另外分发，线下培训，可以自行再中转。
第二步：出题并与模型交互
出题请确保题型在下述范围内，并符合分布的偏序关系。
对于高度相似的题目（环境几乎相同+任务几乎相同），我们将只接收一条，并拒收剩余条目。
若模型未在首轮对话后完成任务，需要修复错误或追加需求，专家可以继续与模型在当前会话窗口内进行交互，但至多不超过 10 轮。
需要单独针对【每一轮对话】按第三步的五个维度分别打分，并在对应的「- 描述」字段里写明扣分依据。对话满 10 轮后，无论模型是否完成任务，都不应继续引导，应创建新的任务对话，开始新的题目。
特别说明：
对于显然的工程故障（例如网络波动/模型不稳定导致请求失败），不计入轮次。
但是，对于思考次数超限需要人为“继续”的情况，应计入轮次。
整体任务类型的分布偏序关系如下：0-1代码生成 / Feature迭代 / Bug修复 > 代码理解 ≈ 代码重构 > 其他
按天维度进行统计，这意味着每天提交的数据的题型分布都应该达到上述标准。
任务类型
0-1 代码生成	基于 PRD 或自然语言描述的新项目/完整模块构建
Feature 迭代	在已有功能基础上的平滑扩展
Bug 修复	逻辑修复、报错排查、安全漏洞修补
代码理解	解释逻辑、寻找潜在风险、回答技术细节
代码重构	性能优化、可读性提升、解耦
工程化	环境配置、依赖管理、CI/CD 相关
代码测试	单测、集成测试等
任务难度的分级如下表。需要专家根据repo、技术栈、题目从人类执行的视角判断任务难度。
任务难度
简单	任务意图明确，主要沿已有模式做机械修改或局部实现；即使改多个文件，只要模式高度一致、核心判断很少，也算简单。
请注意：首轮任务中，严禁出简单题。但是后续轮次中，可能因为模型产物质量不佳而产生较为简单的bugfix，这部分数据是允许提交的。
中等	需要理解局部到跨模块的调用/数据流，做一些实现决策；有若干边界条件或约束，但不需要重新设计架构。
困难	需要整合多个模块/系统约束，做关键设计取舍，处理复杂状态、兼容性、权限、并发、性能或异常链路。
地狱	人执行起来需要极高的决策复杂度，通常需求很开放或约束很隐蔽，需要极其深度项目理解、架构级判断、多步复杂的调试验证，或正确性依赖大量边界场景。即使当前的 SOTA 模型也无法成功交付。
第三步：打分并撰写反馈
禁止照抄下面的分数描述，必须结合轨迹中的实证来说明打分原因。
维度	描述	5分 (完美/超预期)	4分 (优秀/小瑕疵)	3分 (及格/勉强可用)	2分 (不及格/需重构)	1分 (完全不可用/严重事故)
交付完整性 (Delivery)
	任务成败：需求功能点是否完整实现？代码能不能跑？有无虚假成功（宣称做完了实际没改或改错）？
	完整实现显式要求及必要边界需求，没有虚假成功。	核心要求达成，只有少量细节问题，没有虚假成功。	核心勉强可用，但有明显 Bug，或有轻微且证据完整的虚假成功。	主要需求没有达成，逻辑或依赖问题导致不能运行，或有较大比例的虚假成功。	完全失败，发生严重编译或逻辑事故，全部不可运行，或有恶劣且证据完整的虚假成功。
指令遵循 (Instruction Following)
	是否严格遵守本轮 User Prompt 及当时有效上下文中的显式、隐含、格式和负面约束，且没有擅自扩展范围？	严格遵循所有有效约束与格式，没有擅自扩展。	核心及绝大部分约束达成，只有少量非关键格式偏差。	遗漏少数明确但非核心的约束，需要局部修改。	违背核心指令或关键负面约束，需要大规模重构。	完全无视约束或行为失控，产物完全不可用。
任务规划 (Planning)
	是否合理拆解任务、清晰推进阶段并持续更新状态；只有关键歧义影响结果时才需要主动澄清，不为制造流程而多余提问。	拆解合理、阶段清晰、持续更新状态，遇到关键歧义主动澄清。	规划和阶段总结良好，只有少量状态更新不及时。	有大方向但拆解不细，状态追踪存在明显遗漏。	缺乏规划、盲目开写，状态与实际不符，需要外部重新整理。	没有步骤和状态追踪，进展不可知。
推理能力 (Reasoning)	依据可见说明、决策、排除过程和产物因果，判断需求理解、根因定位和分析投入是否匹配；不索取或猜测不可见的内部思维。	准确理解需求及因果，复杂问题聚焦根因，简单问题处理直接。	方向正确，能定位大部分根因，偶有冗余。	主要方向可用，但需要多次纠正、忽略边界或分析分散。	逻辑混乱、错误排除或猜测，根因不准，分析明显过多或不足。	严重误解、循环发散或明显失控。
执行能力(Toolcall)	依据可见的文件操作、工具调用、命令和输出，判断操作是否准确精简、是否有无效重复，以及能否根据报错恢复。	工具操作精准精简，少有无效重复，能根据报错恢复。	调用高效，有少量冗余，不影响结果和整体效率。	有明显重复搜索、读文件或多次碰壁，但仍能完成。	有大量冗余失败调用、反复撤销修改，效率低。	工具滥用或缺失，反复使用错误工具导致最终失败。
第四步：提交数据
对于每一轮对话（一个 prompt-response pair），请单独提交一条数据。
如果某一轮次由于工程故障/网络波动，而不具备反馈价值，可以不提交这一轮，除此之外，应将会话中的所有有效轮次进行反馈和提交。
提交完整个任务内的轮次后，应打开新的任务窗口和项目路径（如需），再进行下一道题。
五、质检点
●整体质量维度
数据总量统计
整体合格率
数据多样性（是否覆盖了各场景，是否出现了雷同的题目）
●标注合规性
轮次合规（是否超10轮、窗口拆分是否正确）
字段完整（是否有字段缺失）
题目分类准确性（任务类型、技术栈、难度）
打分与描述一致性（是否在原因中提到产物有问题却标注了任务完成，或反之）
●描述撰写质量
语句通顺，表达清晰完整
是否出现了极其明显的过程及产物问题却偷懒没有写出
是否真正看过了过程与产物
是否定位到问题具体环节
是否指出了具体行为/问题
六、不被允许出的雷同题
在大量质检中我们还发现了一些已经被出烂的题目和项目（各种经典小游戏、todo、CRUD 模板），见下方整理，这些题目我们都会重点查重并拒收。
🎮 游戏 / 图形
贪吃蛇， 打砖块， 俄罗斯方块，坦克大战经典小游戏及其变种
粒子·物理模拟（星系/烟花/落沙/布料）
塔防 / 2D 解谜 / 潜行 / 平台跳跃 
喂食小动物 
记忆翻牌 / 连连看
五子棋等棋类· 2048 · 贪吃蛇 
俄罗斯方块 / 扫雷 / 打地鼠 
🖥️ 本地 / 桌面工具
命令行工具 CLI 
代码片段管理器 
批量重命名 · 截图标注 
文件管理/同步 · 书签 · 密码管理器 
🏢 平台 / 业务系统
电商购物车/订单 
权限 RBAC 后台 · 仓库库存 
投票问卷 · 考勤 OA · 图书借阅 
博客 CMS · 医院挂号/问诊 
外卖点餐 · CRM · IM 私信 
拍卖 · 停车场 · 工单客服 
商品+Excel 导入 · 积分商城 
XXX 预约系统
📊 数据可视化 / 前端页面
报表统计 · Streamlit · CSV 看板 
记账 · 健康健身 · 菜谱 
天气 · 番茄钟 · 习惯打卡 
音乐播放器 · 旅行日记 · 观影记录 
七、其他常见问题
🛠️ 任务定义与轮次逻辑
Q：数据提交是以“轮次”为单位吗？“一轮”的具体定义是什么？ 
A：是的。每一个单轮对话（Single-turn）即为一条独立的数据。
一轮 = 一次交互（用户提问 + 模型回答）。
即使是一个复杂的任务进行了多次对话，也需要拆分成多条数据提交。
Q：任务类型是根据当前轮次确定的，还是整个对话确定的？ 
A：根据当前轮次确定。每一轮的标注都要基于该轮的实际内容。
示例： 第一轮是“代码生成”，第二轮如果是修复第一轮的错误，则第二轮的任务类型应变更为“Bug修复”。
🔄 截断与“继续”处理
Q：对于用户输入了“继续”的轮次，提示词（Prompt）应该怎么写？
A：可以直接写“继续”，但任务类型和难度应综合上一轮原始需求来判断。
Q：对于“继续”后的轮次，评价标准是什么？ 
A：评价标准依然是上一轮用户希望模型完成的任务。
⚖️ 评分与验收标准
Q：数据验收是针对整个多轮对话会话，还是单轮数据？ 
A：针对单轮进行验收，每一个单轮对话是一条数据。
Q：如果模型前几轮翻车，但最后一轮修正了，这几轮该怎么评分？ 
A：
1.前几轮： 通常应该会出现一些低分。
2.最后一轮： 针对这一轮的表现来评估，结合专家个人用户体验。
如果这一轮依然是反复自纠、磕磕绊绊才完成，体感可能较差。
如果是突然开智回到正轨、高效完成交付，即使前面轮次有错，最后一轮也可能出现高分甚至满分的情况。
Q：是否可以接受多模态Prompt？ 
A：我们可以接收多模态数据，如果是多模态，除了prompt之外还需要增加一个字段"prompt附件"上传prompt中的附件，提交上会多一步的时间，不可以因此作为原因而提高AHT，请内部衡量。
🎯 题目多样性与难度要求
Q：对任务的难度上限和下限有要求吗？ 
A：要求如下：
上限： 无量化要求，欢迎高难度挑战。
下限： 有严格限制。禁止提交过于简单的题目。
Q：同一个repo可以出多少题目？
A：不限制，但是禁止雷同的repo+雷同的题目（包括第一期的数据）。
Q：什么样的题目是“过于简单的题目”？
A：同时满足下述两种及以上特征的题目，是过于简单的题目，拒收：
维度	"过于简单"的特征
修改范围	单文件局部修改、纯函数级实现（典型如 LeetCode 题、独立工具函数）
上下文依赖	不需要任何 Repo 上下文、纯文本 Prompt 就能完成
交互轮次	大多数模型 1 轮就能完美交付，不需要看报错、不需要规划
技术广度	单一文件、单一语言、不涉及前后端联调或跨服务
存档：如何写好不满意原因
在提交不满意原因时，请先在脑海中问自己三个问题，想清楚：
1.“模型是从哪一步开始降智的？”
2.“模型的具体什么操作让我觉得很弱智？”
“换做是一个人类高级工程师，遇到这种情况会怎么做？”


“不满意原因”的硬性要求（质检会看）
1.过程与产物都要分析，要分别说清"过程哪里差、产物哪里差"。
2.定位到具体环节和步骤：指明问题发生在哪个步骤、哪个工具调用、哪次文件改动。
3.用证据，不用形容词：引用报错信息、出错的文件名/函数、漏掉的具体需求；禁止只写"效果不好/不行/一般"等笼统的表面症状。
4.只能写由模型本身能力造成的问题，不能写环境/网络波动导致的问题，例如模型请求失败。

写作方法参考
一条合格的原因必须同时具备三要素：
触发节点（When）：在哪个阶段或哪个动作出了问题？
实际行为（What）：模型做错了什么？
业务影响（Impact）：这个错误导致了什么后果？
一条好的原因，除此之外还需要能够指出模型错误行为的根因和正确做法。
一条数据可能存在很多个问题点，每一点都应按三要素展开说明，写作格式无需完全一致（不作为质检点），但是上述硬性要求都需要踩到（作为质检点）。
过程问题： 
在【第几步/哪个环节】，模型【做了什么具体行为】→ 导致【什么后果】（→ 【根因和正确做法】）
产物问题： 
产物在【哪个文件/功能】存在【什么具体问题】，表现为【客观证据，如报错/缺失需求】，无法满足【哪条需求】。
产物问题可能是由过程问题导致的，这时可以是更为完整闭环的表述：
在【第几步/哪个环节】，模型【做了什么具体行为】→ 导致产物在【哪个文件/功能】存在【什么具体问题】，表现为【客观证据，如报错/缺失需求】，无法满足 【哪条需求】，（→ 【根因和正确做法】）。
典型的句子示例：
❌ 不合格（一定会被拒收）	✅ 合格
"没完成任务，不满意"	"prompt 要求支持分页，产物只实现了列表查询，完全没动分页参数，产物不完整。"
"代码有 bug"	"产物 utils/date.py 的 parse() 未处理时区，输入带 Z 的 ISO 串会抛 ValueError。"
"(一段意味不明的报错信息)"	"在第 3 步用 edit_file 改 auth.py 时引入了未定义变量 token，且未运行就宣称完成，导致代码报错。"
"过程比较乱"	"规划混乱：模型在第 2、4、6 步三次重复读取同一个 config.py，且第 5 步改完没运行就宣称完成，存在验证缺失"
"效果还行但不满意"	"任务虽完成，但模型臆造了 lodash.deepMerge（实际不存在该方法，应为 merge），仅因该分支未被执行才没报错，属于是出现了幻觉"
"思考超限需要人工继续"	"效率差：一个单文件改动用了 18 步，其中 9 步是反复试错式的 grep，且触发一次思考超限需人工'继续'"
“文件没改成功，一直在报错。”	“在修改 utils.py 时，模型没有先使用 Read 工具读取文件，就直接尝试用 SearchReplace，导致匹配不到对应的代码而报错，模型应该在修改任何已有文件前，必须先 Read 获取最新的上下文才是合理的。”
“写的代码根本不符合我的需求。”	“任务是‘优化当前页面的加载速度’。模型花了很多步骤去修改后端的数据库查询（实际行为），但我明确在提示词里说过‘只允许修改前端缓存逻辑’（违反的约束）。期望模型在规划阶段（Plan）严格遵守作用域限制，不要越界修改（期望行为）。”
“越改问题越多。”	“在终端返回了 npm install 的版本冲突错误后，模型连续 3 次尝试了完全相同的安装命令。期望它在遇到此类错误时，能主动读取 package.json 检查依赖版本，而不是盲目重试。”
“需求实现得不完整。”	“对结果不满意的原因如下：虽然三个显式条件全部答对，D_i公式、GAE masking、chunk hidden state 都正确，测试写得也规范。但题目要求的 warm-start（soft-switch teacher + behavior cloning）、前6步probe 协议、eval.py 均未实现，deliverable 残缺。此外存在一个未被自己测试捕获的真实 bug:PPO update 时 chunk 横跨 episode 边界不重置 hidden state，与rollout 时行为不一致。
对过程不满意的原因：模型把"注意事项"当 checklist 来对号入座，只处理被显式点名的三条，对需要自己发现的系统性缺失（warm-start 整个训练阶段缺失）没有意识到。是“做对了被考的点"而不是“做完了这个项目”。”

找问题的方向
在读模型过程和产物时，我们需要关心的问题包括但不限于：
过程类常见问题
类别	典型表现
指令遵循	模型是否严格遵守了用户在 Prompt 中明确提出的所有约束条件（如：特定库的使用、代码风格、禁止修改的文件等）？是否存在忽略明确约束、漏做部分需求、做了没要求的额外改动、擅自改技术栈等行为？
任务规划	模型是否能合理拆解任务？是否有清晰的阶段性状态追踪？遇到歧义时是否主动求问？是否有阶段性总结或反馈？是否无规划/规划错误、步骤顺序混乱、目标漂移、来回反复？
工具使用	模型是否能够调用合理的工具？是否出现了参数错误，改文件前不先读、同一失败调用反复重试、不会用搜索等问题？
幻觉	模型是否出现幻觉？是否编造看似合理、实则不存在的 API/参数/配置项，或臆造了 repo 里的文件/函数，声称做了实际没做的操作？
验证缺失	模型是否没有执行合理的测试验证就说完成？是否对报错视而不见、不检查输出？
纠错能力	模型陷入死循环、重复同一种无效修复、或者遇到困难就轻易放弃？
交互沟通	该问不问/不该问乱问、进度汇报误导、缺乏阶段性总结等
产物类常见问题
类别	典型表现
正确性	编译/运行失败、逻辑错误、边界 case 挂、引入回归
完整性	漏需求、半成品、留 stub/TODO
代码质量	可读性差、无错误处理、结构差、违反 repo 规范
过度工程	无意义的高复杂度、多余文件
偷工减料/造假	硬编码、假数据、删/改测试来hack、占位实现
破坏性	删改无关代码、破坏既有功能
安全	引入漏洞
UI/美观度	难看、与设计不符、布局错乱



